apiVersion: v1
kind: Service
metadata:
  name: virbius-engine
  namespace: virbius-system
  labels:
    app: virbius-engine
spec:
  type: ClusterIP
  selector:
    app: virbius-engine
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
